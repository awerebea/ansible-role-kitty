---
kitty_install_dir: $HOME/.local
# kitty_bin_dir: /some/path   # Specify in the playbook to create a symbolic
#                               link to the Kitty binary in this directory.
# kitty_desktop_file_dir: /path   # Specify in the playbook to modify and copy
#                                   the kitty.desktop in this directory.
kitty_download_dir: /tmp/kitty-installer-ansible
kitty_env_files_to_modify: []
